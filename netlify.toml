[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
  VITE_BASE = "/"

# Optional: redirect SPA routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
